


interface Base {
    name: String!
}

type A implements Base {
    name: String!
    a: String!
}

type B implements Base {
    name: String!
    b: String!
}

type Query @generate {
    base: Base
    bases: [Base]
}